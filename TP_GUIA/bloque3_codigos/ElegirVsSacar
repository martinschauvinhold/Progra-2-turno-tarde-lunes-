public class Main {
    public static void main(String[] args) {
        ConjuntoTDA c = new ConjuntoE1();

        c.InicializarConjunto();

        c.Agregar(3);
        c.Agregar(7);
        c.Agregar(3); // no debe repetirse
        c.Agregar(10);

        System.out.println("Al llamar el Metodo Elegir me devuelve:" + c.Elegir());
        System.out.println("Al llamar el Metodo Elegir una segunda vez sin modificar el conjunto me devuelve:" + c.Elegir());
        System.out.println("LLamar el Metodo elegir dos veces seguidas sin llamar al metodo sacar me devuelve el mismo valor");
    }
}
